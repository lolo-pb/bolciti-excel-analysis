# bolciti-excel-analysis

Entry point in app.py (recomended)

But can be run directly from controller.py

Build using
´´´
for windows:
pyinstaller --onefile --windowed app.py

for linux:
pyinstaller --onefile --noconsole app.py

´´´

